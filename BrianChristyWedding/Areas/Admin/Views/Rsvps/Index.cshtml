@model IEnumerable<BrianChristyWedding.Models.Rsvp>

@{
    ViewBag.Title = "RSVPs";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Invitation.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Invitation.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Created)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Updated)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Invitation.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Invitation.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Created)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Updated)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InvitationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InvitationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InvitationID })
        </td>
    </tr>
}

</table>
